# Changelog

## 1.30-ziggy1
- Guaranteed Tank logic is now every chapter/map after survivors leave saferoom.
- Does not force-spawn if a normal director Tank already spawned this chapter.
- Does not force-spawn if a Tank is currently alive.
- Adds one-Tank-alive enforcement.
- Disables duplicate Tank behavior by default.
- Keeps HP control disabled by default so Versus/default HP stays normal.
- Adds Versus-only mode by default.
